<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="account_move_form_view">
            <field name="name">account.move</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <header>
                    <field name="invisible_button" invisible="1"/>
                    <button name="sentInvoice" string="Sent To Revmax" type="object" class="oe_highlight" attrs="{'invisible':[('invisible_button','=',True)]}"/>
                </header>
            </field>
        </record>

    </data>
</odoo>